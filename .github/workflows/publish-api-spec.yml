name: Publish API Spec to Konnect Dev Portal

on:
  workflow_call:

jobs:
  publish-spec:
    name: Publish API Specification
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install yq
        run: |
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq
          yq --version

      - name: Validate OpenAPI Spec
        run: |
          # OpenAPI仕様の基本的な検証
          if [ ! -f "kong/specs/openapi.yaml" ]; then
            echo "❌ OpenAPI Specファイルが見つかりません"
            exit 1
          fi

          # YAMLの構文チェック (yqを使用)
          yq eval '.openapi' kong/specs/openapi.yaml > /dev/null || {
            echo "❌ 有効なOpenAPI仕様ではありません"
            exit 1
          }

          echo "✅ OpenAPI Spec検証完了"

      - name: Publish API Spec to Konnect
        env:
          KONNECT_TOKEN: ${{ secrets.KONNECT_TOKEN }}
          API_PRODUCT_ID: ${{ secrets.API_PRODUCT_ID }}
          # VERSION_IDは指定しない → openapi.yamlのinfo.versionを使用
          KONNECT_API_ENDPOINT: ${{ vars.KONNECT_API_ENDPOINT || 'https://us.api.konghq.com' }}
          SPEC_FILE: kong/specs/openapi.yaml
        run: |
          chmod +x scripts/publish-api-spec.sh
          ./scripts/publish-api-spec.sh

      - name: Summary
        if: success()
        run: |
          # OpenAPI仕様からバージョンを取得
          SPEC_VERSION=$(yq eval '.info.version' kong/specs/openapi.yaml)

          echo "## ✅ API Spec公開成功" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "API仕様がKonnect Dev Portalに公開されました。" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📋 詳細" >> $GITHUB_STEP_SUMMARY
          echo "- **API ID/Name**: \`${API_PRODUCT_ID}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Version**: \`${SPEC_VERSION}\` (openapi.yamlから自動で取得)" >> $GITHUB_STEP_SUMMARY
          echo "- **Spec File**: \`kong/specs/openapi.yaml\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔗 リンク" >> $GITHUB_STEP_SUMMARY
          echo "- [Konnect Console](https://cloud.konghq.com/)" >> $GITHUB_STEP_SUMMARY
          echo "- [APIs](https://cloud.konghq.com/apis)" >> $GITHUB_STEP_SUMMARY
        env:
          API_PRODUCT_ID: ${{ secrets.API_PRODUCT_ID }}

      - name: Failure notification
        if: failure()
        run: |
          echo "## ❌ API Spec公開失敗" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "API仕様の公開に失敗しました。ログを確認してください。" >> $GITHUB_STEP_SUMMARY
